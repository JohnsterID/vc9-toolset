# Consumed via FetchContent_MakeAvailable() — provides vc9_setup() function.
# See vc9-toolset.cmake for details.
cmake_minimum_required(VERSION 3.19)
include("${CMAKE_CURRENT_LIST_DIR}/vc9-toolset.cmake")
